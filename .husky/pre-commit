# Run format check
npm run format:check || exit 1

# Run lint check
npm run lint || exit 1

# Run TypeScript type checking (will fail on type errors)
npm run typecheck || exit 1

# Run tests (will fail if tests fail)
# npm run test:run || exit 1 # No tests yet
