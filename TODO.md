# TODO:

## Must haves:

- The agent's reply UI needs to be as wide as it can in the browser window, and handle markdown and code snippets correctly
- When a message is sent to the agent, the text in the message box should be cleared.
- The up and down arrows when focused on the chatbox should bring up past sent messages in chronological order.
- The title at the top is not necessary.
- The UI closely resembles Google's material UI, let's make it more rounded and use a hotter color (instead of the blue seen on the buttons)
- The UI needs to support both dark and light modes.

## New features:

- Text input, we should be able to press a button and record a prompt.
- Text to voice, we should have a button to have the models replies be read with text to voice.
